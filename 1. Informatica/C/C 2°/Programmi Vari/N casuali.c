#include <stdio.h>
#include <stdlib.h>
#define size 40
int f_casuale(int,int);
void main()
{
    int casuale[size], ripetizioni[10];
    int index, cont;
    srand(time(0));
    for(int i=0;i<10;i++)
        ripetizioni[i]=0;
    for (int i=0;i<10;i++)

}
